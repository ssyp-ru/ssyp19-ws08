class NovelOption {
  String text;
  String next;
  
  public NovelOption(String text, String next) { // Create new instance of NovelOption using text and next
    this.text = text;
    this.next = next;
  }
}
